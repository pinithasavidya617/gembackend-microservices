server:
  port: 8082

spring:
  application:
    name: dealer-service
  datasource:
    url: jdbc:postgresql://localhost:5436/dealerdb
    username: dealer
    password: dealer123
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true