server:
  port: 8082

spring:
  application:
    name: dealer-service
  datasource:
    url: jdbc:postgresql://localhost:5434/dealerdb
    username: dealer
    password: dealer123
  jpa:
    hibernate:
      ddl-auto: update
