server:
  port: 8082

spring:
  application:
    name: trade-service
  datasource:
    url: jdbc:postgresql://localhost:5435/tradedb
    username: trade
    password: trade
  jpa:
    hibernate:
      ddl-auto: update
